{
  "name": "app",
  "private": "true",
  "dependencies": {
    "axios": "^1.4.0",
    "esbuild": "^0.18.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "yarn": "^1.22.19"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds --public-path=assets"
  }
}
